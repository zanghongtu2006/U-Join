server.port: 8080
# ???????
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/imserver
    username: root
    password: example
  # JPA??
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
  redis:
    port: 6379
    host: 127.0.0.1
    password:
debug: true