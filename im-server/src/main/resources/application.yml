server.port: 8080
# ???????
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/chatters
    username: root
    password: example
  # JPA??
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
  redis:
    port: 6379
    host: 127.0.0.1
    password:
debug: true